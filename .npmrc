# EAS Build runs a clean install on its servers. Several of this project's
# native SDKs (LiveKit, Stripe, WebRTC) declare peer ranges that npm's strict
# resolver rejects, so the same flag used locally has to apply there too.
legacy-peer-deps=true
